<body style="background:#80BFFF">
<p id="notice"><%= notice %></p>

<h1 style="color:white">Listing Books</h1>
<table>
  <thead>
 <tr bgcolor="red">
    <tr>
       <th style="width:10%"><h2 style="color:white"> Book
       <th style="width:30%" ><h2 style="color:white">Book name</h2></th>
           <th style="width:20%"><h2 style="color:white">Auther</h2></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr bgcolor="white">
        <td><%= book.book_no %></td>
        <td><%= book.book_name %></td>
        <td><%= book.auther %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h2><%= link_to 'New Book', new_book_path %></h2>
